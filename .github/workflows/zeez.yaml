name: Replicate Jenkins Automation

on:
  push:
    branches:
      - development   # trigger when pushing to development branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Set up Maven
        uses: stCarolas/setup-maven@v4
        with:
          maven-version: '3.9.6'

      - name: Build with Maven
        run: mvn clean package

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build Docker Image
        run: |
          docker build -t actions .
    







      

      # # 6. Log in to Docker Hub
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      

      # # 8. Run the container
      # - name: Run Docker Container
      #   run: docker run -d -p 5050:8080 --name zeez ${{ secrets.DOCKER_USERNAME }}/zeez:latest

      # # 9. Expose via Ngrok
      # - name: Expose with ngrok
      #   uses: ngrok/ngrok-action@v2
      #   with:
      #     ngrok_authtoken: ${{ secrets.NGROK_AUTH_TOKEN }}
      #     port: 5050

      # # 10. Test the endpoint inside GitHub Actions
      # - name: Test container inside runner
      #   run: curl http://localhost:5050 || true
